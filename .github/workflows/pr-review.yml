name: PR Review

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  review:
    uses: YOUR_ORG/.github/.github/workflows/pr-review-reusable.yml@main
    secrets:
      BEDROCK_PROXY_URL: ${{ secrets.BEDROCK_PROXY_URL }}
